{% extends 'base.html.twig' %}

{% block title %}Edit Gallery{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-sm-6 col-md-4 col-md-offset-4">
                <div class="account-wall">
                    <div class="form-group">
                        <h1>Edit Gallery</h1>
                        {{ include('gallery/_form.html.twig', {'button_label': 'Edit'}) }}
                        <a href="{{ path('app.gallery.remove', {id: id, id2:gallery.id}) }}" class="btn btn-primary">Remove from gallery</a>
                        <br>
                        <a href="{{ path('app.campaign', {id: id}) }}" class="btn btn-primary">Back to Campaign</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}